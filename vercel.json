{
  "buildCommand": "pnpm install && sh build.sh",
  "outputDirectory": "artifacts/horizone/dist/public",
  "rewrites": [{ "source": "/api/:path*", "destination": "/api/index" }],
  "env": {
    "BASE_PATH": "/",
    "PORT": "3000",
    "NODE_ENV": "production"
  }
}
