{
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/horizone run build && mkdir -p dist && cp -r artifacts/horizone/dist/public/. dist/",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/",
    "PORT": "3000",
    "NODE_ENV": "production"
  }
}
